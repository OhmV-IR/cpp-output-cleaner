### Jenkins cpp output cleaner

This is basically just a function to take a ninja vcpkg output directory and remove any unnecessary files from it before uploading it as an artifact to reduce build size.
